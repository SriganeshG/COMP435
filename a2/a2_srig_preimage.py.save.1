import os,sys,hashlib,random,string,binascii

hash1 = sys.argv[1]
hash2 = ' '
random = random.getrandbits(32)
print(random) 

while(hash1 != hash
	hash2 = hash_func.hexdigest()
	#hash2 = hashlib.md5(random).hexdigest()	
	#'{:.24}'.format(hash2)
	hash2 = hash2[:24]
	random + str(randint(0,9))

print(hash2)

